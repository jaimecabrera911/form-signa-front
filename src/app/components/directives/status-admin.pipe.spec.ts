import { StatusAdminPipe } from './status-admin.pipe';

describe('StatusAdminPipe', () => {
  it('create an instance', () => {
    const pipe = new StatusAdminPipe();
    expect(pipe).toBeTruthy();
  });
});
