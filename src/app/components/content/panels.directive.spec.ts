import { PanelsDirective } from './panels.directive';

describe('PanelsDirective', () => {
  it('should create an instance', () => {
    const directive = new PanelsDirective();
    expect(directive).toBeTruthy();
  });
});
