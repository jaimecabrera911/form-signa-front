import { ActionsDirective } from './actions.directive';

describe('ActionsDirective', () => {
  it('should create an instance', () => {
    // @ts-ignore
      const directive = new ActionsDirective();
    expect(directive).toBeTruthy();
  });
});
