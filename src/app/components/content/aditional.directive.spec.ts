import { AditionalDirective } from './aditional.directive';

describe('AditionalDirective', () => {
  it('should create an instance', () => {
    const directive = new AditionalDirective();
    expect(directive).toBeTruthy();
  });
});
